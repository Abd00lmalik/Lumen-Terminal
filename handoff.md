# handoff.md; Coding-Agent Continuity

**Audience:** future AI coding agents. **Read time:** ~2 minutes.
**Scope rule:** continuity/context only; NOT architecture, NOT runtime-agent instructions. Do not let this file become a second architecture document.

---

## 1. Project Overview

- **What:** AI Trading Research Workbench; a natural-language research environment for crypto traders. Trader asks in plain language; the system plans research, maintains evidence/hypotheses/judgments, and returns a decision-ready judgment with provenance.
- **Context:** Independent research product (Lumen Terminal). Bitget research skills are a primary tool ecosystem among provider fallbacks.
- **NOT:** not an autonomous trading bot, not a chatbot wrapper, not an execution engine. The trader always makes the final decision.

## 2. Current Repository State

Verified 2026-09-12:

- 39 architecture specification files (`*.txt`) at repo root; **originals, preserved untouched.**
- `docs/architecture/`; the maintained documentation layer: 39 converted `.md` docs (1:1) + `README.md` index + `conversion_log.md` (mapping + fidelity verification). Formatting normalized; content preserved.
- `AGENT.md`; runtime research-agent routing/orchestration layer (complete).
- `FINDINGS.md`; Bitget capability research: verified per-Skill matrix, 8-flow mapping, gaps, adapter implications, risks, proposed phases (complete).
- `tools/convert_architecture.py`; one-time conversion script (kept for re-runs/verification).
- `IMPLEMENTATION_PLAN.md`; implementation plan produced per the final architecture lock (§16); M0-only authorization.
- `handoff.md`; this file.
- **Application code: M0 + M1 + M2 + M3 + M4 + M4b + M5 + REQUIREMENT-COMPLETENESS BENCHMARK EXIST and are tested.** `package.json` + `tsconfig.json` + vitest; `src/domain/` (object model, lifecycle machines, provenance, TOOL_RESULT, evidence classification, workspace aggregate, **thesis + saved artifacts**), `src/persistence/` (memory + file stores), `src/adapters/` (capability-first registry + G1/G2 interfaces with NotConnectedError stubs), `src/research/engine.ts` (capability-routing shell), `src/research/flow1.ts` (Flow 1 pipeline), `src/research/context.ts` (validated epistemic research context), `src/research/adaptive.ts` (model-assisted adaptive research loop), `src/research/requirements.ts` (**requirement-coverage engine**: MACRO_REGIME 6 dimensions, CAUSAL supply+demand, EVENT timing+consensus+fundamentals, evidence quality assessment, causal chain requirements, coverage verdict), `src/model/` (**provider-neutral ModelProvider layer** + validated output schemas + **Gemini adapter**), `src/lui/lui.ts` (**full natural-language LUI pipeline, 6-action model**). **659 vitest tests green (mocked; suite passes with NO GEMINI_API_KEY present) + 25 env-gated skipped (684 total); `tsc --noEmit` clean.** Requirement-completeness benchmark: 38 tests across 7 categories (MACRO_REGIME, SINGLE_ASSET, EARNINGS/EVENT, COMMODITY/CROSS-ASSET, CRYPTO_MARKET, CAUSAL/TRANSMISSION, ADVERSARIAL/UNSEEN) + cross-category integration tests.
- **M1 Bitget adapter layer EXISTS (live-corrected in M2):** `src/adapters/transports/` (resilience primitives: typed failure classification, throttler, bounded retry/backoff, raw capture; `mcp.ts` Bitget public market-data MCP client **with the live-discovered session handshake**; `rest.ts` public Bitget REST client), `src/adapters/bitget-skill-adapter.ts` (generic skill-adapter base: classification, freshness, provenance, per-record error-envelope handling), `src/adapters/bitget-skills.ts` (the five CONFIRMED skills as descriptors + registrations; technical-analysis MCP-first with REST-klines fallback), `src/adapters/freshness.ts` (FINDINGS.md-derived profiles). Five skills registered through the generic registry; no Flow→Tool hardcoding; G1/G2 still vendor-neutral stubs.
- **M2 Flow 1 vertical slice EXISTS:** `src/research/flow1.ts`; request → research target → living plan → capability selection → registry execution → TOOL_RESULT → evidence → claims → contradiction check → analysis → judgment → persistence → concise progressive-disclosure response. Mocked-fixture tests use live-discovered response shapes; live tests run against real endpoints (see `FINDINGS.md` §7b for the 2026-09-13 live-validation results and the 10 recorded discoveries L1–L10).
- **M3 LLM-backed LUI EXISTS:** `src/model/`; provider-neutral `ModelProvider` interface (`structured()` with schema-name + validated output), typed `ModelFailure` taxonomy (PROVIDER_UNAVAILABLE / AUTH_FAILURE / RATE_LIMITED / INVALID_OUTPUT / TIMEOUT / EMPTY_OUTPUT), `validateModelOutput` gate on every model output (strict schemas; invalid output = model failure, never an execution command); `src/model/gemini.ts`; Gemini adapter (REST `generativelanguage.googleapis.com`, credentials ONLY from env `GEMINI_API_KEY`, model via `GEMINI_MODEL`, config-layer default `gemini-3.6-flash`; set during the M3 live verification; key never logged/echoed/persisted; tested); `.env.example` + `.gitignore` (`.env` ignored). `src/lui/lui.ts`; pipeline: normalization → intent (6-action classification via validated model output) → context/target resolution (workspace-grounded; invented ids rejected) → ambiguity check (ambiguous consequential → clarify, no state touch) → consequence check → safety screen (execution-like intent rejected before dispatch) → validated action plan → dispatch (RESEARCH→adaptive loop; ANALYZE; CHALLENGE; MANAGE_STATE; MONITOR→proposal-only; SAVE→confirmation-gated) → progressive-disclosure response (L0–L1 deterministic default, L2+ model-polished but citation-validated; invented citations dropped). `src/research/adaptive.ts`; bounded living loop (MAX_RESEARCH_ROUNDS=3): model-proposed plan (capabilities only, never tools) → engine executes via registry → evidence ingestion → validated CONTINUE/COMPLETE/INSUFFICIENT_EVIDENCE decision → persistence at loop end (store failure propagates). `src/domain/thesis.ts`; Thesis (trader-owned, versioned, system CANNOT revise; throws on non-trader origin) + SavedArtifact. `src/research/context.ts`; research context builder preserving epistemic classes (interpretation/proxy/speculation never presented as observation); tool failures appear ONLY as limitations.
- **M4 research flows EXIST (Flows 2/6/7 only):** `src/research/flow-runner.ts`; ONE shared flow runner (living plan → registry execution → adaptive rounds → flow outcome) with `FLOW_OBJECTIVES` metadata: the flow defines objective + analytical mode (CAUSAL / SYNTHESIS / FALSIFICATION); NO Flow→Tool hardcoding (tested). `src/research/flow2.ts`; causal investigation: event definition from available evidence, competing candidate hypotheses, causal-status ladder (TEMPORAL_ASSOCIATION→CORRELATION→PLAUSIBLE_MECHANISM→STRONG_CAUSAL_EVIDENCE; correlation never asserted as causation), hypothesis linking, leading-vs-competing explanations preserved. `src/research/flow6.ts`; cross-domain synthesis: adaptive scope/dimensions, parallel independent dimension research (bounded), typed disagreement model (GENUINE_CONTRADICTION / DIFFERENT_TIME_HORIZON / DIFFERENT_VARIABLE / EVIDENCE_TYPE_MISMATCH / INTERPRETATION_VS_OBSERVATION / UNRESOLVED_UNCERTAINTY; never forced agreement), research-first-then-synthesize (model background knowledge never presented as evidence). `src/research/flow7.ts`; falsification (the METHODOLOGY behind the CHALLENGE LUI action; distinct, not duplicated): belief decomposition, falsification targets labeled DERIVED_FROM_BELIEF vs PROPOSED (no invented thresholds), disconfirming-evidence bias, materiality ladder (MINOR→MEANINGFUL_WARNING→MATERIAL_CONTRADICTION→INVALIDATING), honest "no credible contradiction found ≠ confirmation", assessment vocabulary reuses the existing judgment model, monitoring is PROPOSAL-only, thesis NEVER mutated. LUI dispatch extended (`src/lui/lui.ts`): flow-classified RESEARCH steps → Flow 2/6; CHALLENGE with falsification objective → Flow 7; unmapped flows keep the M3 adaptive loop. Failure law implemented: provider failure keeps its typed classification (never laundered into INVALID_OUTPUT). Schema-validator addition: `"array"` guard for model arrays whose entries are validated/dropped per-entry by flow parsers (malformed entries dropped, never coerced).
- **M4b research flows EXIST (Flows 3/4/8; completing the 8-flow model except Flow 5):** `src/research/flow3.ts`; Flow 3 WHAT COULD AFFECT IT? (factor landscape; EXPLORATORY mode; factor-status vocabulary OBSERVED_CURRENT_DRIVER / POTENTIAL_DRIVER / CATALYST / RISK / DEPENDENCY / SPECULATIVE_FACTOR as flow OUTPUT structure over existing objects; transmission mechanism + would-matter conditions per factor; missing data ≠ evidence of absence; no prediction/trade signals; tested). `src/research/flow4.ts`; Flow 4 DOES MY THESIS HOLD? (thesis evaluation; EVALUATION mode; reuses the Thesis object's own claims/assumptions/invalidationConditions; claim-level status + evidenceQuality as SEPARATE fields (confidence ≠ evidence quality ≠ assessment); unavailable evidence leans INDETERMINATE, never UNSUPPORTED-by-absence; existing judgment vocabulary; thesis NEVER mutated; tested). `src/research/flow8.ts`; Flow 8 EVALUATE WITH MY FRAMEWORK (framework from SAVED artifacts (type "framework"); criteria are authoritative; QUALITATIVE unless the framework text itself defines scoring; a numeric score with a qualitative framework is REJECTED (tested); INSUFFICIENT_EVIDENCE criteria carry evidenceNeeded; no framework → honest failure, no generic fallback (tested); framework artifact untouched; tested). Shared: `FlowMode` extended additively with EVALUATION + EXPLORATORY; `FLOW_OBJECTIVES` registry extended; LUI dispatch covers all six implemented flows; `LuiResult` gained `flow3`/`flow4`/`flow8`.
- **M5 continuity layer EXISTS:** `src/domain/memory.ts`; research memory (MemoryEntry: categories research/thesis/framework/preference/historical/monitor; decay CURRENT→STALE→HISTORICAL; influence reduced, never deleted; revalidation records outcome + preserves original; conflict law: current validated research > memory, conflicts mark the memory STALE and keep the record) + monitoring handoff (Monitor: PROPOSED→ACTIVE→PAUSED/STALE/COMPLETED lifecycle with illegal-transition rejection; invalidation vs early-warning conditions as DISTINCT kinds; activation rejects non-trader origins; SOURCE_UNAVAILABLE is a state, never a false invalidation). `src/domain/thesis.ts` gained ThesisAssessmentRecord (assessment = research result, never mutation). `src/domain/workspace.ts` gained memory/monitor/assessment stores + snapshot extension + `getContinuitySnapshot()` (full recovery view: active research/branch/judgment/thesis/latest assessment/framework/monitors/memories/uncertainties/contradictions). `src/research/reassess.ts`; reassessment pathway: materiality gate (new data alone never triggers) → model reassessment over thesis+history+evidence+memories → validated history record (thesis untouched) → monitor revalidation proposals (REVIEW flags, never silent change) → memory revalidation (not-confirmed → STALE). `src/lui/lui.ts`: confirmed SAVE now promotes artifact → persistent MEMORY (categorized); MONITOR persists a PROPOSED monitor (inert handoff); SAVE/MONITOR dispatch executes on explicit trader-confirmed request origin, otherwise awaitingConfirmation.
- Git: **not initialized.**

## 3. Source-of-Truth Hierarchy

1. **Architecture** = `docs/architecture/` (the root `.txt` files are the original source, preserved). Architectural truth lives there; read it, don't infer it from here.
2. **`AGENT.md`** = runtime research-agent routing/orchestration knowledge (tells the runtime agent which architecture doc to consult when).
3. **`handoff.md`** = this continuity note for coding agents. Must never duplicate architecture.

Related-but-separate: `FINDINGS.md` = verified Bitget capability research + flow mapping (evidence for tool decisions, not architecture).

## 4. Important Decisions (do not violate casually)

- **8 research flows are locked** (`docs/architecture/research-flows.md`).
- **Natural language is the LUI control surface**; internal ops stay internal (`lui-interaction-model.md`, `lui-flow-extensions.md`).
- **Trader remains the final decision-maker; no autonomous trade execution** (`safety-boundaries.md`).
- **No silent consequential changes**; theses/frameworks/monitoring need explicit confirmation (`safety-boundaries.md`, `core-principles.md`).
- **Capability-before-tool; provider independence**; Bitget-first but never Bitget-only (`tool-skill-orchestration.md`, `data-market-intelligence.md`).
- **Evidence retains provenance; tool output ≠ truth** (`evidence-source.md`).
- **Memory is continuity, not authority**; historical never silently becomes current (`memory.md`).

## 5. Unresolved Questions (all UNRESOLVED; do not decide alone)

- **LUI action-set:** ~~contradiction~~ **RESOLVED by human lock (2026-09-12):** 6 universal actions; RESEARCH, ANALYZE, CHALLENGE, MANAGE_STATE, MONITOR, SAVE (SAVE first-class; distinct from MANAGE_STATE). See `docs/architecture/lui-universal-core.md`; `lui-flow-extensions.md` carries the amendment note.
- **ONCHAIN domain strategy:** accept proxy-level on-chain evidence (verified: market-intel cannot do whale/reserves/ETF-flow data) vs integrate a dedicated on-chain provider (`FINDINGS.md` §5, G3).
- **External provider selection** for verified gaps G1 (historical data) and G2 (web retrieval); deferred to implementation phase.
- **Persistence/storage technology** for research objects; in-memory + file-backed MVP implemented; a durable store decision remains open.
- **Model/provider choice**; Gemini implemented as FIRST provider behind the neutral `ModelProvider` seam (M3); swapping providers requires no core changes. Default model `gemini-2.0-flash`, configurable via `GEMINI_MODEL`.
- **Invocation route** for Bitget skills: install skills into the coding-agent host vs call the public MCP tools directly from our orchestrator (`FINDINGS.md` §7).

## 6. Current Phase

**Implementation phase; M0, M1, M2, M3, M4, M4b, M5, M6(audit), REQUIREMENT-COMPLETENESS BENCHMARK COMPLETE.** The requirement-completeness benchmark (38 tests, 7 categories) proves the core law: research completion is requirement-driven, not evidence-prescent-driven. Key engine enhancements: MACRO_REGIME expanded from 3 to 6 engine-required dimensions (rates, volatility, dollar, growth regime, inflation regime, credit); evidence quality assessment (DIRECT_EVIDENCE / SUPPORTED_INFERENCE / CORRELATIONAL / UNRESOLVED); causal chain requirements for CAUSAL questions; hollow completion check in flow runner; `scopeExclusionToCapabilities()` in adaptive loop. All 659 tests pass, `tsc --noEmit` clean. Remaining: live benchmark (requires `FREEBUFF_LIVE=1`), browser E2E verification, production build + push.

**STOP rule in effect: no further phases without explicit human direction.**

**Developer workflow for Gemini (established in M3):** copy `.env.example` → `.env`, set `GEMINI_API_KEY` (and optionally `GEMINI_MODEL=gemini-3.6-flash`); deterministic tests never need the key; live validation runs via `FREEBUFF_LIVE_GEMINI=1 npx vitest run tests/model/gemini.live.test.ts tests/lui/lui.live.test.ts` (PowerShell: `$env:FREEBUFF_LIVE_GEMINI="1"` first). **Live Gemini integration PARTIALLY verified** (2026-09-13): connection + provider round-trip + 6/10 live LUI scenarios passed; RESEARCH/COMPOUND/six-action batch pending quota reset (free tier: 20 req/day for Gemini 3.x flash family).

## 7. How a New Coding Agent Should Start

1. Read this file.
2. Inspect the repository; verify actual state; don't assume this file is current.
3. If working on the runtime agent: read `AGENT.md`, then the architecture docs it routes to.
4. If working on tools/integrations: read `FINDINGS.md` (verified capabilities, gaps, adapter implications).
5. Read the relevant architecture documents for your task; start from `docs/architecture/README.md` (reading order included).
6. Verify external/Bitget capabilities against official sources rather than trusting docs or this handoff.
7. Check §5 unresolved questions before making architectural changes.
8. Continue from the actual repository state; update `handoff.md` when a major milestone changes it.

## 8. Important Warnings

- **Architecture ≠ implementation.** Specs describe intent; nothing is built.
- **Do not invent unverified Bitget capabilities.** Label findings CONFIRMED / INFERRED / UNKNOWN / GAP (`FINDINGS.md` shows the format).
- **Do not silently resolve architectural contradictions**; get human approval. (The LUI 5-vs-6 action issue was resolved by an explicit human lock on 2026-09-12.)
- **No credentials, API keys, wallet phrases, or private keys in the repository**; ever.
- **Do not casually redesign locked architecture.** The 39 root `.txt` files are deliberate originals; the `.md` tree mirrors them 1:1.
- Filenames (both root `.txt` and converted names) can mislead; check contents; root files contain special characters (em dashes, `#`, `→`) so quote paths.

## 9. Handoff History

**2026-09-22; transmission/causal-link layer (node evidence is not arrow evidence)**
- **Why:** a question asking how an oil move *transmitted through inflation, yields and risk assets* produced a fluent story over oil/yield/equity evidence with NO inflation evidence, and still read HIGH confidence. The nodes were evidenced; the arrows were assumed.
- **Contract (`src/research/requirements.ts`):** `transmissionTargetsOf` / `causalLinksOf` fold the markets named in the question's own transmission wording onto the shared market vocabulary (`TRANSMISSION_CLAUSES` grammar; unseen assets of a known class behave identically). Every named target becomes a required dimension: its own `TRANSMISSION` row (`targetTerms`) when the ledger did not already ask about that dimension, otherwise the arrow is **attached to the existing dimension row** (`transmissionTargets`) — one row per dimension, one arrow per named link. `TRANSMISSION_TARGET_FOLDS` + attribute-fold filter (`NON_MARKET_FOLDS`) keep "higher oil prices" from inventing a PRICE party. A link-bearing question also gets a material-alternative-explanations requirement (`asksForAlternatives` promotes it to CORE when the question asks whether the explanation could differ).
- **Subject gate widened exactly as far as the question's wording:** `admittedTargetsOf(req)` returns the requirement's own declared link targets, and `matchRequirement` unions them in per requirement — so evidence about a target the question named is admitted, no other dimension is exempted, and no caller-side wiring can drop the exemption. Ingestion and context probes carry `targetTerms`/`transmissionTargets`/`relationshipType`.
- **Engine-derived link status (`src/research/causal.ts`):** `deriveCausalLinkStatuses` reads the ledger (satisfied + direct/multi-source → `SUPPORTED`; correlational or single-source → `PARTIALLY_SUPPORTED`; stale-only → `STALE_ONLY`; exhausted/unavailable → `UNRESOLVED`; still pending → `NOT_RESEARCHED`) and `weakestCausalLink` names the binding arrow.
- **Consequences wired:** `confidence.ts` caps confidence by the weakest link (unresolved/stale/unresearched → LOW) and reports `causalLinks`/`weakestCausalLink`; `contract-checks.ts` rejects **assertive** causal prose about a non-supported arrow (`CAUSAL_CLAIM_WITHOUT_LINK_EVIDENCE`, hedged language stays admissible) — the contract-state ledger now carries the link fields plus `evidenceQuality`/`sourceDiversity`; `renderRequirementCoverage` states each arrow's status and the link law to the synthesis model; `researchDiagnostics` exposes `causalLinks` + `weakestCausalLink`.
- **Frontend:** `ActiveResearchPage` gained a collapsed "What Lumen checked" panel (question type, coverage, engine-computed confidence, gate, per-role requirement counts, transmission links with the weakest arrow called out, and the requirement ledger + confidence basis behind a disclosure). The trader-facing answer stays the main surface.
- **Tests:** new `tests/research/transmission.test.ts` (24 tests: target parsing incl. no-false-causality, one-row-per-dimension, alternatives, subject-gate width, link statuses, causal-claim validation, confidence cap, watch-next as a CORE dimension, negative contamination with an abundant-but-irrelevant evidence pool); decision-quality benchmark gained a 16th dimension `CAUSAL LINK VALIDATION` (a question that named no transmission must derive NO links) and a `CROSS-DOMAIN TRANSMISSION` case where the missing inflation leg must stay unresolved, bind the weakest link, cap confidence at LOW, and block `EVIDENCE_SUFFICIENT`; `tests/api/api.test.ts` asserts the diagnostics link surface (and that an "affecting" question derives none).
- **Verified:** backend `tsc --noEmit` clean, **681 deterministic tests green** (25 env-gated skipped), frontend typecheck + production build clean, 42 frontend tests green. Live/browser verification not re-run in this pass (deploy-then-manual verification agreed with the human).
- **Determinism defect found and fixed while verifying:** `EquityFundamentalsAdapter`/`EarningsCalendarAdapter.ensureCrumb()` called the GLOBAL `fetch` for the Yahoo cookie/crumb handshake, bypassing the injected transport seam — the deterministic suite passed or failed depending on live network reachability (observed: two runs failing in `tests/adapters/equity.test.ts` + `failure-injection.test.ts`, other runs green). `RestTransport.fetchImpl` is now public and both adapters handshake through it; the `fakeRest` double serves the crumb, and a regression test stubs the global fetch to throw so a future re-introduction fails loudly.
- Docs updated: `docs/architecture/research-contract.md` (transmission link section + diagnostics surface).

**2026-09-21; research-contract hardening pass (roles, challenge law, deterministic confidence)**
- **Research contract (generic; no question list):** `questionTypeOf` (COMPARISON/CAUSAL/EVENT/MACRO_REGIME/THESIS/FALSIFICATION/HISTORICAL/SYNTHESIS) + `subjectMarketClassOf`/`subjectClassOfKind` read the question's decision shape and market class; `completeRequirements` ADDS any omitted engine-required dimension (`engineRequired: true`, with declared `evidenceClasses` and optional `calculation`). Every ledger carries a CHALLENGE requirement. Role decides requirement identity (a CORE requirement containing "weakening" is not the CHALLENGE dimension). Causal supply/demand dimensions apply only to commodity/metal subjects. Wired in `runAdaptiveResearch` after plan parsing.
- **Role completion laws:** CONTEXT never blocks; CHALLENGE blocks until attempted (`markChallengeAttempted` — only a `DISCONFIRMATION_CAPABILITIES` run counts) or the deployment has no disconfirmation route (`markUnattemptableChallenges` → UNAVAILABLE with blocker); recovery maps a blocking CHALLENGE to disconfirmation capabilities only; `mandatoryCapabilities` skips CHALLENGE (the counterevidence floor owns it).
- **Admission gates share one matcher:** the synthesis-context requirement probe now carries `engineRequired`/`evidenceClasses` (previously dropped, so the two gates disagreed about the same item → hollow-complete). Class matching applies to engine requirements with subject-scoped evidence only.
- **Deterministic confidence** (`src/research/confidence.ts`): computed from core coverage, stale-only, challenge attempt, missing calculations, failed paths, honest-gap state; caps the synthesis model's stated level and is exposed with its basis in `researchDiagnostics` (+ questionType, requirementRoles). LUI `validateCitations` now also accepts run-evidence ids (admitted evidence was previously filtered out of citations).
- **Requirement-scoped retrieval:** recovery rounds dispatch with `retrievalBrief(objective, blocking)` (requirement, window, role, evidence classes); deep-research agents receive the unresolved-requirement brief, not just the question.
- **Benchmarks:** decision-quality suite extended to 15 dimensions (CHALLENGE EXECUTION added; CORE scored on role rows) + thesis/falsification/historical/cross-asset/held-out-platinum scenarios; process harness registers the production FALSIFICATION tier. New unit suites: `tests/research/requirements-contract.test.ts`, `tests/research/confidence.test.ts`. Docs: `docs/architecture/research-contract.md`, `requirement-engine.md`, `recovery.md`, `docs/benchmark/decision-quality.md`.
- Verified: backend 619+ tests green (incl. the two new suites), frontend 42 green, both typechecks + production builds clean, secret/trading scans clean. Flow-runner (Flows 2–8) does NOT yet join the requirement ledger/floor/gate lifecycle — known remaining gap.

**2026-09-21; research coverage contract (engine-owned requirements, capability floor, benchmark visibility)**
- **ENGINE-OWNED CAPABILITY FLOOR (`mandatoryCapabilities` in `src/research/requirements.ts`, wired in `src/research/adaptive.ts` round 1):** the model still proposes capabilities, but it can no longer OMIT one an engine-derived CRITICAL requirement maps to (lookup against the existing `CAPABILITY_SUPPORT` catalog). Live failure fixed: "what is driving Treasury yields higher" planned only a news capability, so no yield observation was ever retrieved. The floor also always attempts `FALSIFICATION` for analytic questions (counterevidence as an engine action, not a prompt convention), is bounded (≤4 extra capabilities), and is filtered by registry availability so it can never spend a round on a capability no provider serves.
- **RECOVERY BEFORE INSUFFICIENCY:** the model's `INSUFFICIENT_EVIDENCE` decision previously ended the run immediately. It now goes through the engine's coverage assessment first: blocking CRITICAL requirements with untried mapped capabilities schedule a bounded recovery round; only after recovery is spent does the model's insufficiency stand (with the ledger marked EXHAUSTED and a reason). "Insufficient" now means the relevant registered paths were exhausted.
- **DECLARED SUBJECTS TRAVEL INTO COVERAGE:** `CoverageEvidence` gained the declared `subject` (`about`), and `matchRequirement` matches over text + subject. Market payloads frequently never name their own ticker ("the 10-year Treasury yield is 4.998 percent" for `^TNX`), so valid observations for symbol-shaped subjects were dropped at coverage time.
- **AVAILABILITY IS A FILTER in `capabilitiesForRequirement`.** It previously only affected ranking, so recovery could schedule a capability with no registered provider (a guaranteed empty round).
- **BENCHMARK VISIBILITY (API):** the research response now carries `researchDiagnostics` — the requirement ledger (description, importance, time horizon, status, evidence/stale counts, recovery attempts, unresolved reason), every capability execution with its provider/completeness/failure type, the floor capabilities, recovery rounds, the completion gate, and an engine-assessed `coverage` (COMPLETE/PARTIAL/INSUFFICIENT). Structured facts and provenance only, no model reasoning. The answer also carries `counterevidenceStatus` (PRESENT | NONE_FOUND | NOT_ASSESSED) so an empty opposition list cannot read as balance that was never searched for.
- **Verified:** backend `tsc` clean, **587 deterministic tests green** (51 files; new `tests/research/capability-floor.test.ts` covers the floor, no-duplication, disconfirmation, recovery-before-insufficiency, and honest exhaustion); frontend typecheck + build clean, 42 frontend tests green; secret/trading scans clean.
- **LIVE (this pass):** macro risk-assets, oil drivers, copper, gold and the dollar now retrieve question-scoped evidence with zero crypto contamination; the dollar question previously dispatched asset `USD` (Yahoo serves that as ProShares Ultra Semiconductors) — the earned-asset law now dispatches the index the question names. Treasury-yields live re-check after the floor landed is listed in the report; the pre-floor yields run is the known weak case (one deep-tier agent payload, LOW confidence).
- **Still open:** frontend browser E2E (Playwright absent), per-requirement retrieval-objective web recovery (Phase 6 of the contract), and the full 10-question live benchmark record.

**2026-09-16 (final); production deployment, provider fallbacks, UI professionalization, repo cleanup**
- **PRODUCTION IS LIVE (VERIFIED LIVE):** https://asklumen.vercel.app; the whole blocker was (a) no `/api` functions on Vercel and (b) the SPA resolving to `http://127.0.0.1:3001` in production. Fixed with a ~40-line Vercel function adapter (`api/research.ts`) hosting the SAME Fastify app via `buildApi().app.routing` (no second backend, no engine changes), `vercel.json` (SPA rewrites excluding `/api`, 300s function duration, no-store API headers), same-origin `/api` resolution in `client.ts` (regression-tested), and encrypted server-side env vars (`GEMINI_API_KEY`, `GEMINI_MODEL`) on the Vercel project (name: **lumen**; the domain `asklumen.vercel.app` is attached to it and auto-deploys from `main`). Live production tests: health 200; Flow 1 COMPLETED with REAL Bitget technical evidence (RSI 45.35) on the serverless API; SSE streaming works.
- **Registry-owned provider fallback (VERIFIED LIVE from production):** NEWS → curated public crypto RSS (secondary reporting, per-item publisher/time/URL); SENTIMENT → alternative.me Fear & Greed (`SENTIMENT_SIGNAL` + explicit proxyBasis, never upgraded); MACRO → World Bank indicators (`QUANTITATIVE_OBSERVATION`, freshness STALE). Failover triggers on typed failure OR empty coverage (live-discovered defect: Bitget-from-Vercel returns valid-but-empty payloads; the registry only fell over on throws). Serving fallback preserves an `attemptedProviders` audit trail + human-readable limitation; all-fail/all-empty stays an honest EMPTY. 12 deterministic fallback-law tests; Bitget remains PRIMARY (used for technical analysis in the same production run).
- **Frontend boundary suite (new, 11 tests):** URL resolution law (prod = same-origin, never localhost), NetworkError environment vocabulary (production message contains no dev instructions), shared `BackendDownNote` rendering, single-fetch-boundary scan. All 8 pages now use the shared notice.
- **UI professionalization (no redesign):** surface-type system (§ judgment / metric / source-ref / diagnostic / episode surfaces), judgment as the visual focal point (elevated surface, restrained accent, larger verdict type), evidence source-ref chips + event timestamps, home hero with example-question chips that seed the real ask-bar, **Flow 5 structured presentation**; new `historicalAnalysis` DTO field (server-computed episode analysis: current setup, per-dimension matched/differed chips, forward-outcome windows) rendered as a timeline-style historical-research view, never derived client-side.
- **Serverless persistence honesty:** `createProductionStore`; MemoryStore unless `WORKSPACE_FILE` is explicitly set; DEPLOYMENT.md documents the upgrade path behind the two-method `WorkspaceStore` interface. Typed credential-absence test (missing GEMINI_API_KEY names the variable, never a value).
- **Repo cleanup:** all 40 root `.txt` architecture sources removed (fully converted to `docs/architecture/*.md`; conversion log updated); design reference image → `docs/design-reference.png`; stale logs untracked; `.vercel/` gitignored. Root now reads as a real project.
- **Verification:** 389 backend + 11 frontend deterministic tests green; both typechecks + frontend build clean; secret/mock/trading/fetch scans clean. Docs updated: README (production live + fallback section + fallback Mermaid), DEPLOYMENT.md (deployed architecture, env vars, persistence honesty), BENCHMARK_REPORT.md (production verification + 4 new defects with fixes). Known caveat: one G2 test is load-sensitive under full-suite parallel load (passes standalone; noted, not weakened).

**2026-09-16 (earlier); G1 analysis layer, hardening, UI verification, GitHub release**
- **Flow 5 analysis layer complete** (`src/research/episode-analysis.ts`): deterministic episode detection over the real G1 candle record (10-day windows), **explainable similarity** (per-dimension matched/differing breakdown; never a bare score), forward outcome windows (1/3/7/14d) computed separately from detection (**look-ahead protected**; dedicated failing-if-leaked test), current-setup extraction, same-calendar-date dedupe, and non-maximum suppression so overlapping windows don't masquerade as independent analogues. Response renders CURRENT SETUP → HISTORICAL ANALOGUES → WHAT THIS DOES NOT ESTABLISH. Precedent is never prediction.
- **CONFIRMED DEFECTS found & fixed this phase (all regression-tested):** (1) Flow 5 attached no Judgment object (only flow without one); now records a deterministic judgment (MODERATE only with comparable episodes; explicit "precedent ≠ prediction" wording); (2) cross-round evidence duplication; repeated identical capability calls re-ingested the same outputs (live: 111 blocks for 37 real); fixed in the shared runner via invocation+output signature dedupe; (3) **restart ID collision (data loss)**; fresh process counters restarted at 1 and OVERWROTE persisted objects (live: rs_000001 clobbered); fixed via `seedIdCountersFromIds` in `Workspace.fromSnapshot`; (4) runs killed by a restart stayed ACTIVE forever; startup sweep (`ResearchApp.create`) marks judgment-less ACTIVE research STOPPED with an honest note.
- **UI verification PASS (VERIFIED LIVE, backend-side proof):** real Chrome via raw CDP typed a Flow 5 question into the real ask-bar → new research object rs_000007 with 37 evidence chunks + judgment; earlier false-failures of the harness were diagnosed (SSE submission dies when the page is force-reloaded mid-run; failed runs persist as judgment-less ACTIVE objects; the sweep now relabels them STOPPED).
- **G2 web/primary-source retrieval implemented** (`src/adapters/g2-web-retrieval.ts`): bounded, SSRF-guarded URL validation, HTML→text extraction, source classification (primary/secondary/commentary/community), duplicate-origin non-corroboration, source/evidence separation (a page is a source, not automatically evidence), failure ≠ negative evidence. 14 deterministic tests + 1 env-gated live test. Registered in `createBitgetAdapterSet` (capability-first; engine selects when a plan needs it).
- **Verification:** 372/372 deterministic tests, backend + frontend typecheck clean, frontend production build clean, secret/mock/trading/fetch scans clean (one fetch boundary). Live Flow 5 E2E: COMPLETED with 37 monthly chunks / 1,095 real daily candles → 5 analogues from 118 candidates → judgment. Honest note: one G2 test flaked once under full-suite parallel load (passed standalone + on re-run; no assertion weakened).
- **Release:** README.md (Mermaid architecture/LUI/evidence/Flow-5/thesis diagrams, honest limitations), DEPLOYMENT.md (frontend deploys to Vercel; backend requires a long-running host + persistent volume; no false serverless claims), BENCHMARK_REPORT.md updated. Git initialized; pushed to github.com/Abd00lmalik/Lumen-Terminal.


**2026-09-12**
- Architecture specification phase → preparation/research phase completed: 39 architecture `.txt` files converted 1:1 to `docs/architecture/*.md` (originals preserved; fidelity verified); runtime `AGENT.md` created; official Bitget Skills researched and verified (5 Skills, no-key public MCP data, per-Skill detail in `FINDINGS.md`); 8-flow capability matrix + gap analysis + findings report produced; concise `handoff.md` maintained.
- One architectural contradiction resolved by explicit human lock (2026-09-12): LUI action set = 6 actions incl. SAVE. Root `.txt` originals untouched; amendments live in the `.md` layer.
- Next decision: ~~human review of `FINDINGS.md`~~ → **RECEIVED:** implementation approved per final architecture lock; vertical slice = Flow 1; MVP adapters = G1 interface + G2 interface (vendor selection deferred); no dedicated on-chain provider (proxy evidence must stay explicitly labeled).

**2026-09-15 (later same day); Benchmark, reliability & end-to-end validation complete**
- Human authorized the benchmark phase: audit the integrated product, build a serious benchmark suite, fix real defects, verify fixes. No frontend redesign; no architecture changes.
- **Benchmark suite created (spec `BENCHMARK.md`, report `BENCHMARK_REPORT.md`):** deterministic (Suite A), env-gated live (Suite B, quota-aware via `BENCH_SCENARIOS=` filter), browser E2E via raw CDP (Suite C, gated on API health + `FREEBUFF_LIVE`). Verification categories kept separate: VERIFIED LIVE / VERIFIED DETERMINISTICALLY / MOCKED / UNVERIFIED / BLOCKED-EXTERNAL.
- **CONFIRMED DEFECT found & fixed; Flow 5 fall-through:** `HAS_THIS_HAPPENED_BEFORE` fell through LUI dispatch into the generic adaptive loop, where current-data capabilities could silently answer a HISTORICAL question (fabricated historical research). Fix at the correct layer: `HAS_THIS_HAPPENED_BEFORE` registered in `FLOW_OBJECTIVES` with HISTORICAL mode; new `src/research/flow5.ts` constrains the shared runner to the G1 stub (throws NotConnectedError → registry → honest UNAVAILABLE). Flow 5 now dispatches through the normal flow path and fails honestly until a G1 vendor connects. Regression tests: `tests/research/flow5.test.ts` (5 tests incl. no current-data laundering + future G1-connected path). Architecture unchanged (one shared runner, no Flow→Tool hardcoding).
- **Suites A–C:** Suite A (11 tests); consolidated anti-laundering probe through the real pipeline (all evidence classes, tool-failure≠negative-evidence, partial-result limitations, missing-provenance rejection, proxy labeling, stale labeling, memory/monitor guards). Suite B (live, quota-aware); SAFETY ✅ VERIFIED LIVE (real Gemini rejected "Buy BTC and open a 10x long" pre-dispatch), FLOW5 ✅ VERIFIED LIVE (honest UNAVAILABLE with real Gemini), TA ✅ VERIFIED LIVE (COMPLETED with real BTC/USDT evidence: RSI 33.34/34.93, MACD golden cross +75.58, Bollinger %b 0.95; conflicting MACD interpretations preserved as genuine disagreement; no forced synthesis). NL/ETH scenarios BLOCKED-EXTERNAL (free-tier daily quota consumed). Suite C; **true browser E2E pass**: real Chrome via CDP typed into the real ask-bar, submission created a NEW backend research object (count 5→6 verified via API), rendered output verified. Two earlier false-pass vectors were caught and fixed in the harness itself (label-text collision with persisted workspace state; sidebar "New research" button matching the ask-bar submit selector); the harness now requires backend-side proof of submission.
- **Flakiness hardened (test-infra, no assertion weakening):** explicit timeout budgets on the two full-engine integration tests; MCP throttle-slot test margin widened so the assertion tests the throttler, not machine load. Two consecutive full-suite green runs confirmed.
- **Scans:** secret scan clean (no GEMINI/Bitget/privkey patterns in src/tests/frontend); mock.ts remains deleted; exactly one `fetch(` boundary; trading/execution scan clean. Note: this workspace has **no git repo initialized**; `.env` is untracked by any VCS.
- Verified: `tsc --noEmit` clean; **328/328 deterministic tests, 24 env-gated skipped** (+16 benchmark tests); frontend typecheck+build clean.
- **Not yet built (next):** G1 historical-data vendor selection (unblocks Flow 5 for real); or demo preparation. Do not proceed without human go-ahead.

**2026-09-15; Gemini free-tier reliability + real end-to-end research verified**
- Human authorized: make the Gemini provider reliable for real E2E research on the free tier. No architecture changes; six LUI actions, flows, and epistemic rules untouched.
- **Quota diagnosis (live, not guessed):** the 429s were `GenerateRequestsPerDayPerProjectPerModel-FreeTier`; 20 requests/DAY for `gemini-3.6-flash` on this key, not per-minute overload. Free-tier daily buckets are PER-MODEL, so a lighter model gets its own fresh bucket. Direct probes of 41 key-accessible models: `gemini-2.5-*` closed to new users (404); `gemini-3.5-flash` + `gemini-3.5-flash-lite` return 200 with valid structured JSON. **Selected `gemini-3.5-flash-lite`** (smallest model that performs LUI classification/clarification/planning; configurable via `GEMINI_MODEL` in `.env`/`.env.example`; documented rationale inline).
- **Retry refinement (`src/model/gemini.ts`):** daily-quota 429s now FAIL FAST (typed RATE_LIMITED, retriable=false; no multi-hour stall); per-minute 429/5xx/network stay retriable with backoff + Retry-After. Prompt contract adds "required list fields: send [] rather than omit".
- **Structured-output robustness for small models (additive, tested):** `validateModelOutput` normalizes absent/null REQUIRED list fields to `[]` (Flash-Lite omits `questions` when there are none; the only honest reading is "none"); scalars stay strictly required; parsers still reject empty lists where emptiness is illegal. Prior fix retained: explicit `null` on optional fields = "not provided".
- **Evidence-path fix (`src/adapters/bitget-skills.ts`):** MARKET_DATA/SENTIMENT adapters previously sent `action:undefined` when the engine planned capability-only params (hub: `Unknown action:` → laundered into EMPTY with no failure). Now default to valid requests (`global` / `long_short`+BTCUSDT); explicit params still win. Regression-tested through the fake transport.
- **Live infrastructure findings (honest, unfixable in code):** `api.bitget.com` REST is network-unreachable from this dev machine (HTTP 000); the public MCP hub (`datahub.noxiaohao.com`, server `market-data-mcp` v1.26.0) is ALIVE and its `technical_analysis` tool returns real data; but its CoinGecko/news/DeFi/social/sentiment upstreams were failing during testing (ConnectTimeout / `items: []` / empty error envelopes). This is FINDINGS.md **risk R1** (single public endpoint) active in practice.
- **REAL E2E verified end to end (browser→…→frontend path exercised earlier; here via API):** "What is the current technical setup on BTC/USDT…" → request_accepted → intent_understood (RESEARCH, real Gemini) → target/ambiguity/consequence/safety checks → research plan → **TECHNICAL_ANALYSIS completed: COMPLETE** with REAL indicator data (RSI 33.34, MACD golden cross +75.58 hist, Bollinger %b 0.95, MA trend mixed) → 2 evidence items (measurement + skill interpretation properly split; provenance, freshness, limitations preserved) → COMPLETED, confidence HIGH, honest keyUncertainty. Two further questions (macro, positioning) COMPLETED with honest insufficiency; their upstreams were down, no fabricated evidence, no negative-evidence laundering.
- Verified: backend `tsc` clean; **312/312 deterministic tests** (2 new) + 18 env-gated skipped; frontend `tsc`+build clean; secret/mock/trading scans clean; single fetch boundary intact; source-count DTO reviewed; not required by API_CONTRACT.md/architecture, left out (frontend renders —, no client-side derivation).
- **Not yet built (next):** monitoring infrastructure (real workers), G1 historical vendor (REST path blocked from this network; Flow 5 stays unavailable). Do not proceed without human go-ahead.

**2026-09-14; Frontend ↔ backend integration complete (mock data removed; real F0 API wired)**
- Human authorized integration only: remove ALL mock data, wire the existing visual layer to the real research agent. No redesign; all 10 routes and the visual system preserved.
- **Mock data deleted:** `frontend/src/data/mock.ts` removed entirely; zero production references (grep-verified). Empty/failed/partial backend states now render honestly (empty states, typed error notes, MODEL_FAILURE cards); never replaced by fictional content.
- **Frontend API layer (`frontend/src/api/`):** `types.ts` (contract types mirrored from `src/api/dto.ts` + API_CONTRACT.md), `client.ts` (single fetch boundary: base URL via `VITE_API_URL` defaulting to `http://127.0.0.1:3001`, typed error normalization `ApiError{code,message,httpStatus,confirmation}`, `NetworkError`, manual SSE parser; fetch+ReadableStream because EventSource cannot POST), `research.ts` (JSON + SSE submit, history), `workspace.ts` (session/continuity), `evidence.ts` (evidence/claims/hypotheses/judgments), `thesis.ts` (reads + domain-boundary selection only), `memory.ts` (read-only), `monitor.ts` (grouped reads + explicit activation). All pages import from `api/index.ts`; no scattered fetch calls (grep-verified: exactly one file contains `fetch(`).
- **Adapters, not redesigns:** `frontend/src/data/adapters.ts` maps DTOs → the existing view types verbatim (epistemic class/freshness/proxyBasis/memory status/monitor lifecycle preserved AS DATA; missing data renders as —/unknown, never invented). `data/types.ts` extended additively (RAW_DATA class, status fields, whatWouldChange/unresolved on assessments).
- **Wired pages:** ResearchWorkspace (NL ask-bar → POST /api/research; real answer card/supporting-opposing reasons/limitations/evidence/judgments; AWAITING_CONFIRMATION → explicit confirm-and-resend with `confirmed:true`; context rail = continuity snapshot), ActiveResearch (REAL SSE progress: every stage line is a backend `progress` event with its stage id; capability chips appear only on actual `capability_started/completed`; the only timer is real elapsed time; final result renders the backend DTO), Evidence (live evidence with class filters + per-object provenance refs, event timestamps, tool refs), Thesis (real thesis + full assessment history incl. what-would-change/unresolved; selection via domain boundary), Challenge (workspace hypotheses as falsification targets; honest NONE_FOUND rendering; assessment + what-would-change), Memory (artifacts + memory with CURRENT/STALE/HISTORICAL demotion), Monitor (lifecycle groups; activation POSTs to the trader boundary then refreshes; no fake background anything), Home (real research history with isCurrent, active thesis, monitor counts, contradictions/uncertainties), Settings (real /api/health only; keys server-side), Landing (static; footer updated to reflect live data). `AppShell` footer/badge updated (mock data → F0 API).
- **Streaming hook:** `frontend/src/hooks/useResearchStream.ts`; SSE consumption mapped to the existing run-stage/capability visuals; stages shown ONLY when the backend emits them; no simulated terminal output, no fake percentages.
- **Additive backend change (defect-grade resilience gap found during E2E):** `GeminiProvider` was single-shot; transient Google-side 503/429 overload aborted research despite being retriable per the M1 failure law. `structured()` now retries TRANSIENT conditions (network/timeout/5xx/429; `retriable===true`) with bounded exponential backoff (default 3 attempts, 1.5s base, injectable sleep); permanent failures (AUTH/INVALID/EMPTY) propagate on the first attempt; the last typed error is rethrown unchanged. 2 deterministic tests added (retry-succeeds + no-retry-on-permanent). `tests/model/provider.test.ts` existing status-mapping tests pin `transientRetry:{attempts:1}` (mapping, not timing, is their subject).
- **Real E2E performed (not just compilation):** backend started via env-sourced `.env` (`GEMINI_API_KEY`/`GEMINI_MODEL=gemini-3.6-flash`, never printed); SSE stream showed `request_accepted` → `intent_understood: RESEARCH` (real Gemini classification success) → typed failure handling on provider overload; browser-driven CDP test typed a question into the real ask-bar, clicked Research, and the UI rendered the backend's honest MODEL_FAILURE card (no fabricated evidence). Model-independent paths verified live: /api/health, /api/workspace honest empty snapshot, /api/research [], /api/monitors grouped empties, 400 INVALID_REQUEST on empty message, all 9 app routes rendering real (empty) state in headless Chrome.
- **Environmental blocker (UNRESOLVED, not code):** the free-tier Gemini key exhausted its per-minute quota during testing; even trivial direct calls now return 429. A full COMPLETED research run was not obtained in this window; the path up to and including real model classification + honest failure rendering is proven, and the provider retry makes transient overload recoverable once quota resets. Re-run `npm run api` + retry to see a completed run.
- **Verification:** backend `tsc --noEmit` clean; **308/308 deterministic + 18 env-gated skipped (1 new retry test)**; frontend `tsc` clean; `npm run build` clean; secret scan clean (no keys in frontend, only `src/api/client.ts` contains `fetch(`); trading-term scan clean; all routes verified rendering live data.
- **Limitations:** source counts are not part of the research DTO (rendered as —); per-research confidence lives on judgments (home list shows UNKNOWN until opened); SSE stream errors after a partial stream surface as connection-lost; free-tier Gemini quota is the practical ceiling on demo frequency.
- **Not done (next phases):** Flow 5/G1, monitoring workers, UI redesign, auth. Next recommended step: re-verify a COMPLETED research run when Gemini quota resets, then polish progressive-disclosure views (L1–L5) against real objects.

**2026-09-14; Frontend visual layer complete (mocked prototype; no backend wiring)**
- Human authorized the frontend visual layer only. `frontend/` scaffolded (Vite + React 19 + TS strict, HashRouter): **10 routes**; landing, home, research workspace, active-research progress, evidence, thesis, challenge, memory, monitor, settings; sharing an app shell (collapsible sidebar, topbar, context rail) and reusable primitives (`components/ui.tsx`: EpistemicBadge, ConfidenceMeter, EvidenceRow, FreshnessDot, Panels, EmptyState…).
- **Design system extracted from the supplied reference image** (pixel-sampled): near-black canvas `#050607`, hairline-bordered panels (no gray slab cards), 60/30/10 hierarchy, teal `#01D4C8` primary accent rationed to <0.3% of pixels per screen, trading-semantic red/green for contradiction/support, Space Grotesk + Inter + IBM Plex Mono. Signature element: the **epistemic rail** (2px color-coded left rail on every research object).
- **Mock data only** (`src/data/mock.ts`, typed in `src/data/types.ts` mirroring API_CONTRACT.md shapes for a later DTO swap). Zero fetch/axios/EventSource/WebSocket calls (grep-verified); no secrets; no trading/execution UI (grep-verified; only the landing disclaimer names them). Responsive drawers <1100px/<900px; reduced-motion respected.
- Verified: frontend `tsc --noEmit` clean; `npm run build` clean; all 10 routes render distinct content (headless-Chrome captures + pixel analysis: ~90% dark canvas, rationed accents, no overflow); backend suite re-run after build: **307/307 + 18 env-gated skipped, zero regressions**.
- Limitations: purely visual; no API calls, no auth, mocked state only. Next integration step: replace `src/data/mock.ts` with calls to the F0 API (`API_CONTRACT.md`) per FRONTEND_IMPLEMENTATION_PLAN.md F2–F4.

**2026-09-14; F0 API boundary complete (Fastify seam over the engine; no frontend built)**
- Human authorized F0 only. Baseline confirmed before starting (285 mocked tests green; typecheck clean).
- **API seam (`src/api/`):** `dto.ts` (safe view-model DTOs + pure mappers; epistemic class/freshness/proxyBasis/memory status/monitor lifecycle preserved AS DATA; provenance as refs; no reasoning/payloads/secrets), `errors.ts` (typed transport errors: 400 INVALID_REQUEST / 404 NOT_FOUND / 409 AWAITING_CONFIRMATION / 503 MODEL_FAILURE / 500 PERSISTENCE_FAILURE / 500 INTERNAL_ERROR with path+secret sanitization), `sse.ts` (typed SSE envelope: progress|final|error), `research-app.ts` (application service; validates input, derives trader origin SERVER-SIDE, calls Lui.handle, persists via the existing WorkspaceStore where engine mutations ended; zero research logic), `routes.ts` (thin Fastify routes), `server.ts` (app factory + dev-locked CORS localhost:5173/4173 + truthful health endpoint), `main.ts` (CLI: env-only Gemini + real Bitget registry + file store; `npm run api`).
- **Endpoints:** POST /api/research (natural language ONLY; no flow parameter can reach the engine; optional `confirmed` flag mirrors the frontend confirmation dialog; `?stream=1` SSE), GET /api/workspace (continuity snapshot DTO), GET /api/research(/:ref) with explicit `isCurrent`, evidence/claims/hypotheses/judgments reads, GET /api/thesis(/:ref) + POST /api/thesis/select (domain `setActiveThesis` only; unknown refs → 404), GET /api/assessments, GET /api/memory + /api/artifacts (READ-ONLY; no persistMemory shortcut exists by construction), GET /api/monitors grouped by lifecycle + POST /api/monitors/:ref/activate (domain trader boundary), GET /api/health (process availability only; no false provider/monitoring/trading signals).
- **Real SSE progress (additive engine change):** `src/research/progress.ts` defines the stage vocabulary; `adaptive.ts`, `flow-runner.ts`, and `lui.ts` accept an OPTIONAL `onProgress` listener and emit events at ACTUAL lifecycle transitions (request_accepted → intent_understood → … → research_plan_created → capability_started/completed → response_ready). No listener = byte-identical prior behavior (285 tests unchanged). No event carries model reasoning/prompts/raw payloads/secrets.
- **Error model law:** in-request typed model failures return 200 with `outcome: MODEL_FAILURE` + typed `modelFailure` (failure ≠ fabricated evidence, partial ≠ false success); transport-level failures map to 503 MODEL_FAILURE. Provider failures surface as `limitations`, never negative evidence.
- **SAVE authorization:** unconfirmed SAVE → `AWAITING_CONFIRMATION`, NOTHING persisted (tested via /api/artifacts + /api/memory returning empty); `confirmed: true` → dispatch through the EXISTING LUI boundary (artifact + categorized memory created). No HTTP shortcut around the confirmation boundary.
- **Tests: 22 new deterministic** (`tests/api/e2e-api.test.ts` 4: full HTTP→LUI→engine→registry→evidence→DTO seam + persistence-across-instances via FileStore + Flow-4 judgment/assessment via API + SSE lifecycle stream; `tests/api/api.test.ts` 18: request handling incl. flow-smuggling ignored, DTO epistemic safety, continuity exposure, SAVE boundary, thesis read+select-only (PATCH/PUT rejected), monitor proposal≠active + SOURCE_UNAVAILABLE as state, no-execution-endpoints, no stack/paths/secrets in errors). **Totals: 307 mocked + 18 env-gated live; typecheck clean; zero M0–M6 regressions.** Secret scan clean; `.env` still ignored; no frontend scaffolded (verified: no `frontend/` dir, no React/Vite/CSS).
- **Dependencies added:** fastify@^5, @fastify/cors@^10 (runtime); tsx (dev, for `npm run api`).
- **Not yet built (next per plan):** F1 app shell + research workspace → F10; Flow 5 still blocked on G1 vendor; no monitoring workers.

**2026-09-14; M6 system audit, E2E validation, frontend architecture (no frontend built)**
- Human authorized M6 audit only. Baseline 269/269 confirmed before starting; 285/285 after (16 new E2E + audit-regression tests; zero regressions).
- **Audit defects fixed (all small, architecture-preserving, regression-tested in tests/research/e2e-scenarios.test.ts):** D1; `set-active-thesis` was validated but never applied and no active-thesis selection persisted (continuity guessed via updatedAt): added `Workspace.setActiveThesis/getActiveThesis` (unknown refs rejected, snapshot round-trip), dispatcher applies the validated proposal, and the research context now exposes the trader's theses inventory so the model can resolve WHICH thesis (selection-only, trader-owned). D2; successful MANAGE_STATE answered "produced no research outcome": dedicated truthful response branch (working state only, memory untouched). D3; Flow 4 evaluations never entered the auditable assessment history: now recorded via recordThesisAssessment (thesis object still never mutated). D4; assessment-history vocabulary was type-unsound (lifecycle union smuggled via cast): first-class `ThesisAssessmentStatus` (SUPPORTED/WEAKENED/MATERIALLY_CHALLENGED/UNSUPPORTED/INDETERMINATE).
- **E2E scenarios (deterministic, fakes only):** S1 basic research + tool-failure-is-not-negative-evidence; S2 ambiguous consequential halts with state untouched; S3 Flow 6 typed disagreement preserved; S4 thesis lifecycle (D3 regression + materiality-gated reassessment + irrelevant evidence no-op); S5 SAVE vs memory boundary + memory-does-not-outrank-current-research; S6 monitor proposal-only, trader-only activation, SOURCE_UNAVAILABLE ≠ invalidation; S7 insufficient evidence completes honestly; S8 execution boundary (safety screen, schema-level action rejection, registry structurally exposes no order/transfer/leverage/position capability).
- **Documents produced:** SYSTEM_AUDIT.md (18 sections: architecture map, request/state/provenance/evidence/thesis/memory/monitor lifecycles, flow matrix, failure matrix, confirmation matrix, security review, 4 CONFIRMED DEFECTS fixed, 8 ARCHITECTURAL GAPS, 7 PRODUCT/UX GAPS, intentional limitations, recommendations); FRONTEND_ARCHITECTURE.md (Vite+React+TS, no state lib; React Query, Fastify F0 API seam, SSE progress, DTO view models preserving epistemic status, Gemini/Bitget server-side only, 8 screens, security boundaries, MVP scope/non-goals, migration path); FRONTEND_IMPLEMENTATION_PLAN.md (F0–F10 milestones with acceptance criteria + tests). NO frontend scaffolded (audit harness constraint respected).
- **Limitations:** Flow 5 still blocked on G1 vendor; monitor state remains persistence-only; memory retrieval still tag-scoped; frontend not built (next phase, per F0–F10).
- Human approved M4b; authorized M5 only. Baseline confirmed before starting (237 mocked tests green; zero regressions after).
- **Research memory** (`src/domain/memory.ts` + workspace): the six architecture categories; SAVE is the only promotion path (confirmed SAVE → artifact + categorized memory entry; ordinary research never auto-promotes; tested); decay (CURRENT/STALE/HISTORICAL) reduces influence without deletion; entries remain listable with auditable statusReason (tested); revalidation records outcome + timestamp while preserving the original content/provenance (tested); conflict law (M5 §18): current research wins, memory marked STALE with the validation relationship recorded, historical record never overwritten (tested).
- **Thesis assessment history** (`ThesisAssessmentRecord` in thesis.ts + workspace): assessment is a research RESULT; recorded with thesis version, status (existing judgment vocabulary), evidence refs, unresolved, whatWouldChange, confidence, and researchQuality as a SEPARATE field; the thesis object is untouched by assessment (tested); requires a real thesis (no fabricated targets; tested); round-trips through persistence (tested).
- **Monitoring handoff** (no infrastructure): Monitor objects with PROPOSED→ACTIVE→PAUSED/STALE/COMPLETED lifecycle, illegal transitions rejected (tested); invalidation vs early-warning conditions recorded as DISTINCT kinds (tested); **activation rejects non-trader origins**; a system/model origin cannot activate (tested); SOURCE_UNAVAILABLE is a recorded STATE, never interpreted as thesis invalidation (tested); review flag (M5 §15) marks STALE without deleting or rewording conditions (tested).
- **Reassessment pathway** (`src/research/reassess.ts`): materiality gate first; irrelevant new evidence is an honest no-op with NO reassessment call (tested); material evidence → model reassessment over thesis + assessment history + new evidence + relevant memories → system-validated assessment recorded to history (thesis untouched; tested) → monitor revalidation (invented monitor/memory refs dropped; tested) → memory revalidation (not-confirmed memories lose current influence but are preserved; tested). Provider failure at the gate = typed failure + honest no-op (tested).
- **LUI integration:** confirmed SAVE → artifact + persistent memory (M5 §4); unconfirmed SAVE → awaitingConfirmation with NOTHING persisted (tested); MONITOR → persistent PROPOSED monitor with distinct condition kinds, nothing activated (tested); SAVE ≠ MANAGE_STATE proven by test (save creates memory but no state change; state change creates no memory); continuity context recovery works from persisted workspace state alone (new LUI instance, no conversation memory; tested). **Design refinement (M5 §7):** a SAVE/MONITOR step gated for confirmation now dispatches when the request ORIGIN itself records explicit trader confirmation ("confirmed" in trader-origin detail); otherwise it halts at awaitingConfirmation exactly as before; M3 tests unchanged.
- **Tests: 32 new deterministic** (memory 7, monitoring 5, assessment history 4, continuity 2, reassessment 8, LUI continuity 6). **Totals: 269 mocked + 18 env-gated live; typecheck clean; zero M0–M4b regressions.** Secret scan clean; no trading/cron/UI/monitoring-infrastructure surface introduced.
- **Limitations:** memory retrieval is tag/thesis-scoped (semantic/vector retrieval deferred; architecture leaves storage implementation open); monitors are persistent handoff state only (no workers/cron; future); reassessment is invoked explicitly rather than auto-scheduled (correct per M5 §13 materiality law; a scheduling layer is future monitoring infrastructure); Flow 5 + G1/G2 remain future work.
- **All planned phases (M0–M5) are now complete. Remaining future work:** Flow 5 (needs G1 vendor), G1/G2 vendor selection, real monitoring infrastructure, UI; each requires explicit human direction.

**2026-09-13; M4b research flows complete (Flows 3/4/8; all flows except Flow 5 now implemented)**
- Human approved M4; authorized M4b only. Baseline confirmed before starting (194 mocked tests green; zero regressions after).
- **Flow 3 WHAT COULD AFFECT IT?** (`flow3.ts`): factor-landscape investigation with EXPLORATORY mode. Factor-status vocabulary (OBSERVED_CURRENT_DRIVER / POTENTIAL_DRIVER / CATALYST / RISK / DEPENDENCY / SPECULATIVE_FACTOR) authored as flow OUTPUT structure over the existing evidence/analysis/judgment objects; no new domain enums. Each factor carries transmission mechanism + would-matter conditions (influence stays conditional); supporting AND contradicting refs; missing data recorded as unresolved/missing, never as evidence of absence; response explicitly states "not predictions". No price prediction, probability, or trade-signal output anywhere (tested with regex sweep).
- **Flow 4 DOES MY THESIS HOLD?** (`flow4.ts`): thesis evaluation in EVALUATION mode. Reuses the Thesis object's OWN claims/assumptions (with importance + invalidationConditions) as the evaluation structure; nothing reinvented. Claim-level assessment carries `status` (existing judgment vocabulary) and `evidenceQuality` (STRONG/MIXED/WEAK/UNAVAILABLE) as separate fields; confidence ≠ evidence quality ≠ assessment (tested with a HIGH-confidence/WEAK-evidence case). Unavailable evidence → UNAVAILABLE/INDETERMINATE, never UNSUPPORTED-by-absence; retrieval failure surfaces as limitation, never contradiction (tested). The thesis's own invalidation conditions are checked with current trigger status. Thesis object NEVER mutated (version stays 1; tested); revision remains a separate confirmed action.
- **Flow 8 EVALUATE WITH MY FRAMEWORK** (`flow8.ts`): framework resolved from SAVED artifacts (the existing SAVE path, type "framework"); no framework → honest failure with NO generic fallback (tested). Framework criteria are authoritative; scoringUsed=QUALITATIVE unless the framework text defines scoring, and a numeric `frameworkScore` with a qualitative framework is REJECTED at validation (tested). INSUFFICIENT_EVIDENCE criteria carry `evidenceNeeded`. CONTRADICTED distinguished from NOT_SATISFIED. Framework artifact byte-identical after evaluation (tested).
- **Shared architecture (additive only):** `FlowMode` extended with EVALUATION + EXPLORATORY; `FLOW_OBJECTIVES` entries added (guidance lives with each flow file); LUI RESEARCH dispatch now covers six implemented flows (Flow 5 remains blocked on G1); no new LUI actions; the locked six unchanged; flow classification stays internal routing.
- **Tests: 43 new deterministic** (Flow 3 ×10, Flow 4 ×12, Flow 8 ×13, cross-flow ×8: shared-runner proof, no-Flow→Tool-hardcoding, registry-boundary, evidence-rule reuse, failure laws, no silent thesis/framework mutation, no autonomous execution). **Totals: 237 mocked + 18 env-gated live; typecheck clean; zero M0–M4 regressions.** Secret scan clean; no trading/UI/monitoring/G1/G2 surface introduced.
- **Limitations:** Flow 5 blocked on G1 vendor (unchanged); live M4b round-trips not exercised against Gemini (free-tier quota consumed during M3 verification; re-runnable after reset); framework criteria parsing is model-mediated from the artifact text (no structured framework schema exists; the artifact is free text by design of SAVE).
- **Not yet built (next):** M5 continuity (thesis lifecycle UX, monitoring handoff, memory, workspace presentation, reassessment) and Flow 5 after G1 vendor selection. Do not proceed without human go-ahead.

**2026-09-13; M4 research flows complete (Flows 2/6/7 on the shared engine)**
- Human verified the live Gemini integration; authorized M4 only. Baseline confirmed before starting (156 mocked tests green).
- **One shared flow runner** (`src/research/flow-runner.ts`); not three pipelines (mandate §4): flow = objective + analytical mode metadata (`FLOW_OBJECTIVES`); capability selection stays with the engine via the registry; flow-specific scheduler bias lives in the objective/mode, not in hardcoded routes (tested: only planned capabilities execute; unmapped flows keep the M3 adaptive loop).
- **Flow 2 WHY DID IT HAPPEN?** (`flow2.ts`): event definition from available evidence only; multiple competing candidate hypotheses (explicit living objects; model-generated ≠ fact); evidence seeks support AND contradiction; causal status ladder distinguishes temporal association / correlation / plausible mechanism / strong causal evidence / inconclusive; correlation is never asserted as causation; leading vs competing explanations preserved; output structure per mandate §10 (what happened / leading explanation / evidence / competing / contradictions / confidence / uncertainty / what would change).
- **Flow 6 WHAT DOES ALL THE INFORMATION SAY?** (`flow6.ts`): "all the information" = all MATERIAL information (adaptive scope/dimension planning; not every capability blindly); independent dimension research runs in parallel with bounded concurrency; typed disagreement model preserves cross-domain conflicts (interpretation-vs-observation typed explicitly; interpretation does not win by confidence); qualitative weighting per the existing evidence model; research-first-then-synthesize (model background knowledge is never presented as researched evidence).
- **Flow 7 WHAT COULD PROVE ME WRONG?** (`flow7.ts`): CHALLENGE (LUI action) vs Flow 7 (research methodology) distinction maintained (mandate §23); thesis decomposed into claims/assumptions (trader-owned, never mutated; tested); falsification targets labeled DERIVED_FROM_BELIEF vs PROPOSED; disconfirming-evidence search bias; materiality ladder (one weak source ≠ invalidation); "nothing found" is explicitly NOT confirmation; assessment reuses the existing judgment vocabulary (SUPPORTED/WEAKENED/MATERIALLY_CHALLENGED/UNSUPPORTED/INDETERMINATE); monitoring proposal-only.
- **Validator extension** (`src/model/provider.ts`): new `"array"` primitive guard for model arrays whose compound entries are shape-checked per-entry by flow parsers (malformed entries dropped, never coerced). Latent bug found by tests and fixed in flow6: a schema key listed in `optional` must still be declared in `properties` (strict-schema check otherwise rejects valid model output).
- **Typed-failure law implemented (M4 §33):** provider failures during flow synthesis/assessment keep their classification (e.g. PROVIDER_UNAVAILABLE); never laundered into INVALID_OUTPUT; validation failures remain distinct; partial research still yields limited honest judgments.
- LUI dispatch extended for flow-classified requests; `LuiResult` gained optional `flow2`/`flow6`/`flow7` results; final-response builder converts flow outputs to the progressive-disclosure shape without re-synthesis.
- **Tests: 38 new deterministic** (Flow 2 ×9, Flow 6 ×7, Flow 7 ×12, cross-flow ×10: routing, compound decomposition, capability-first execution, no Flow→Tool hardcoding, failure semantics, safety, persistence). **Totals: 194 mocked + 18 env-gated live; typecheck clean; zero M0–M3 regressions.** Secret scan clean; no trading/UI/monitoring/G1/G2 surface introduced.
- **Limitations:** flows 3/4/8 and 5 not built (per mandate); Flow 5 waits for the G1 historical adapter; live M4 round-trips not exercised against Gemini (free-tier 20 req/day quota consumed during M3 verification; re-runnable after reset); flow-level contradiction detection remains model-mediated (heuristic keyword-class hints only) pending deeper QC integration in M5.
- **Not yet built (next):** M4b (Flows 3/4/8), Flow 5 after G1 vendor selection, then M5 continuity (thesis lifecycle, monitoring handoff, memory, workspace presentation, reassessment). Do not proceed without human go-ahead.

**2026-09-13; M3 LLM-backed LUI complete (provider-neutral model layer + natural-language research control)**
- Human approved M2; authorized M3 only. No `GEMINI_API_KEY` in the dev environment → implemented with scripted fakes per mandate §22; live path ready via `.env`.
- **Model layer** (`src/model/`): `provider.ts`; `ModelProvider` interface, typed `ModelFailure` taxonomy, strict `validateModelOutput` gate (fenced/prose JSON extraction; unexpected properties rejected), env-only config helper. `schemas.ts`; 15 validated structured-output schemas (normalized request, target, ambiguity, consequence, action plan, research plan, adaptive decision, analysis, challenge, thesis assessment, monitor proposal, save proposal, state change, safety screen, final response) + typed enum parsers (unknown LUI action → error). `gemini.ts`; Gemini adapter: REST transport, `responseMimeType: application/json`, key only in `x-goog-api-key` header (tested: body never contains it), HTTP status → typed failure mapping without echoing response bodies, timeout → TIMEOUT, empty candidates → EMPTY_OUTPUT.
- **LUI** (`src/lui/lui.ts`): full pipeline per mandate §5; all six locked actions dispatch; compound requests decompose into ordered validated steps; ambiguity blocks consequential actions; SAVE halts without trader-confirmed origin (tested: nothing persisted silently); MONITOR is proposal-only; execution-like intent rejected pre-dispatch (tested); invented citations dropped from analysis/response objects; failure responses are honest (model failure ≠ research failure ≠ persistence failure).
- **Adaptive loop** (`src/research/adaptive.ts`): model-proposed validated plan (capabilities only) → registry execution → evidence ingestion → validated CONTINUE/COMPLETE/INSUFFICIENT_EVIDENCE; contradiction-driven second round tested; round budget (3) exhausts honestly; tool failures enter the context only as limitations (never negative evidence; tested); persistence at loop end; store failure propagates.
- **Domain additions** (`src/domain/thesis.ts`): Thesis (versioned; `reviseThesis` throws on non-trader origin; tested) + SavedArtifact; workspace snapshot extended (additive; round-trip tested).
- **Test totals: 156 mocked + 7 env-gated live** (5 Bitget `FREEBUFF_LIVE=1`; 2 Gemini `FREEBUFF_LIVE_GEMINI=1`), typecheck clean, zero M0/M1/M2 regressions.
- **Live Gemini verification (2026-09-13, post-M3 review):** real API connection SUCCEEDED through the full M3 architecture; provider round-trip validated via the schema gate; live LUI round-trips passed for THESIS (no thesis mutation), CHALLENGE (falsification result), SAFETY (execution command rejected pre-dispatch; no execution capability exists in the registry; probed), SAVE (no silent persistence), MONITOR (proposal-only), and evidence handling (epistemic classes + limitations preserved). RESEARCH/COMPOUND round-trips and the six-action batch were interrupted by **free-tier quota exhaustion (429: 20 req/day for the Gemini 3.x flash family)**; a data-availability condition, recorded per failure-recovery law (not a negative finding). Schema gap found and fixed by live verification: `RESOLVED_TARGET_SCHEMA` wrongly required `asset/flow/researchRef` (now declared `optional`; deterministic fakes had masked it). Model default is `gemini-3.6-flash` (config layer only); `GEMINI_MODEL` override verified working (`gemini-flash-latest` used for fallback verification). SECURITY: the key was initially misplaced in `.env.example`; moved to `.env` (never echoed), `.env.example` restored to placeholders; **the exposed key should be rotated**. Repo remains non-git; `.env` gitignored; secret scan clean.
- **Not yet built (next):** M4 flows (2/6/7 first, reusing the research engine), Flow 5 after G1 vendor, then M5 continuity. Live RESEARCH/COMPOUND + six-action batch re-runnable once quota resets (or on a billing-enabled key). Do not proceed to M4 without human go-ahead.

<!-- Future agents: append a dated entry here when a major implementation milestone occurs. -->

**2026-09-15 (latest); G1 historical-data vendor selected, implemented, and live-verified; Flow 5 now produces real historical research**
- Human approved G1 = **Bitget REST candles primary → Binance Vision public-mirror fallback**. Selection was evidence-driven, not documentation-driven: live probes from this machine showed Binance/Bybit/OKX/Coinbase/Kraken/Bitget-API all network-blocked (HTTP 000), CoinGecko anonymous capped at 365d (401 beyond), CryptoCompare now key-gated (401); `data-api.binance.vision` (Binance's official Market-Data-Only URL; no auth, public market data only) serves REAL multi-year OHLCV. Data correctness spot-checked against known history (2021-05-19 crash bar: O 42849.78 / L 30000 / C 36690.09).
- **G1 adapter (`src/adapters/g1-historical.ts`):** implements the M0 `HistoricalDataProvider` interface unchanged. OHLCV real via both venues (Bitget v2 candles primary, Vision klines fallback; forward pagination, dedupe/sort, MAX_PAGES=5 deep-range bound); `funding`/`open_interest`/`liquidations` return honest UNAVAILABLE (mirrored nowhere reachable; never fabricated); monthly-chunk packaging (packaging only, every candle verbatim) after a live run showed 2190 single-candle evidences flooding the research graph; capability defaults for `{asset}`-only generic-loop calls (reliability-phase precedent; explicit params win); venue served travels in `transport` provenance; reuse of RestTransport resilience primitives (throttle/retry/raw-capture).
- **Flow 5 wiring (`src/research/flow5.ts`):** engine-owned `HistoricalQuery` envelope (symbol from resolved asset, metric ohlcv, 3-year daily lookback; overridable for tests); invocation params are an engine decision, never model- or client-decided; deterministic coverage summary (candle count, span, min-low/max-high with exact dates; arithmetic, no interpretation).
- **API-DTO defect found & fixed:** `ResearchApp`'s flow-outcome loop exposed evidence for flows 2/3/4/6/7/8 but omitted `flow5` (written later); Flow 5 evidence never reached the frontend. Regression test added in `tests/api/e2e-api.test.ts`.
- **REAL E2E verified live (API path, real Gemini):** "Search historical data for similar BTC setups and patterns" → COMPLETED, 37 monthly OBSERVATION blocks / 1095 real daily candles (2023-09-17 → 2026-09-15), freshness HISTORICAL, `sourceRefs` carrying raw-capture refs of the actual fetches, eventTimestamp = exact candle close. NOTE: flash-lite routing of Flow-5 phrasings varies run-to-run (one run classified into the generic loop and synthesized a Flow-6-style answer from failed capabilities; honestly reported as failures, nothing laundered); routing itself is model-mediated by design and was previously live-verified for the canonical phrasing.
- Verified: backend `tsc` clean; **341/341 deterministic tests** (+14: 11 adapter, 1 envelope, 1 API-DTO regression, 1 wiring update) + 24 env-gated skipped; frontend `tsc`+build clean; secret/mock/trading scans clean; single fetch boundary intact (2 fetch calls, both in `client.ts`); probe scripts cleaned up.
- **Not yet built (next):** Flow-5 episode-similarity ANALYSIS over the retrieved record (currently a deterministic coverage report; the model layer can now be asked to identify analogous episodes from real candles); G2 web retrieval; monitoring infrastructure. Do not proceed without human go-ahead.

**2026-09-13; M2 Flow 1 vertical slice complete (live endpoints exercised)**
- Human approved M1; authorized M2 only (Flow 1, smallest capability set: news-briefing + technical-analysis).
- **Live endpoint discoveries (details in `FINDINGS.md` §7b, L1–L10):** the MCP endpoint requires a session handshake (initialize → mcp-session-id → notifications/initialized); every tool requires an `action` enum argument (M1's empty-args assumption failed); `news_feed` returns a JSON array of `{feed, error, items[]}` with `limit` 1–10; the MCP server exposes a working `technical_analysis` tool (symbol `BTC/USDT`); per-record error envelopes (`{"cpi":{"error":""}}`) are common; `api.bitget.com` REST is unreachable from the dev environment (recorded, not fabricated around); live news upstream returned 44 feeds / 0 items on the day.
- **Adapter corrections (smallest necessary, evidence model tightened; not weakened):** `McpTransport` session handshake + re-handshake on session loss; action-args mapping for all four MCP skills; news feed-array flattening with feed-error → UNAVAILABLE/PARTIAL; technical-analysis split MCP-first/REST-fallback with leaf-level, type-aware observation/interpretation splitting (verdict fields never become observations; `suggested_stop` always judgment); all-error envelopes → UNAVAILABLE outputs.
- **Flow 1 pipeline** (`src/research/flow1.ts`): full path from request to judgment with capability-first selection, evidence/claims/contradictions/analysis/judgment, persistence, concise progressive-disclosure response; graceful INSUFFICIENT_EVIDENCE completion that asserts no explanation when a capability dimension is unavailable (failure ≠ negative evidence).
- Two additive domain changes (lock preserved): `Evidence.sourceRefs` override in `evidenceFromToolResult` options; `Workspace.ingestEvidence` to register pre-classified evidence without re-creation.
- Verified: typecheck clean; **108/108 mocked tests** (12 new Flow 1 tests with realistic live-shape fixtures); **5/5 live tests with `FREEBUFF_LIVE=1`** (handshake, response shapes, TA splitting, honest end-to-end INSUFFICIENT_EVIDENCE with full research structure + persistence). Live run recorded honestly; no fabricated evidence.
- **Not yet built (next):** M3 LUI (6-action model, intent interpretation, context/target resolution, ambiguity/confirmation boundaries), then M4 flows 2/6/7. G1/G2 vendor selection still deferred. Do not proceed to M3 without human go-ahead.

**2026-09-12 (later same day); architecture lock + M0**
- LUI action-set contradiction resolved by explicit human lock: 6 universal actions incl. SAVE (first-class, distinct from MANAGE_STATE). Amendments in the `.md` layer; root `.txt` originals untouched.
- `IMPLEMENTATION_PLAN.md` produced and checked against the architecture before coding (per lock §16).
- **M0 complete:** TypeScript/Node ESM runtime; research object model + per-object lifecycle machines + provenance; normalized TOOL_RESULT (interpretation never silently becomes observation); evidence classification incl. mandatory proxy labeling; workspace aggregate enforcing one-ACTIVE-judgment supersession with preserved history; memory+file persistence; capability-first registry (no Flow→Tool hardcoding) with G1/G2 stubs that refuse rather than fabricate.
- Verified: `npm run typecheck` clean; 38/38 behavioral tests pass (lifecycle, provenance, normalization, classification, proxy labels, staleness, failure/partial, contradictions, hypothesis living-state, judgment supersession, historical separation).
- **Not yet built (next):** M1 Bitget adapter layer (MCP + REST transports, 5 skill adapters, throttling) → M2 Flow 1 vertical slice → M3 LUI (6-action model). G1/G2 vendor selection still deferred. Do not proceed to M1 without human go-ahead per phasing.

**2026-09-13; M1 Bitget adapter layer complete**
- Human approved M0 + `IMPLEMENTATION_PLAN.md`; authorized M1 only.
- **Transports** (`src/adapters/transports/`): `resilience.ts` (typed `TransportError` failure taxonomy per failure-recovery.md §10; `Throttler` with synchronous slot reservation; `withRetry` bounded exponential backoff + full jitter, Retry-After honored, permanent failures propagate un-retried; `RawCapture` provenance ring buffer); `mcp.ts` (Bitget public market-data MCP client: JSON-RPC `tools/call` over streamable HTTP, SSE-tolerant, no auth per FINDINGS.md §1, timeout → TIMEOUT, isError:true → retriable PROVIDER_ERROR); `rest.ts` (public Bitget REST client for candles; failed/non-JSON responses still raw-captured for provenance).
- **Skill adapters:** `bitget-skill-adapter.ts` (generic base: capability→tool mapping, narrative→ANALYST_INTERPRETATION vs JSON/data→observation-class classification, neutral "data temporarily unavailable" → UNAVAILABLE outputs with PARTIAL/EMPTY completeness, freshness assessment, proxy labeling at the boundary); `bitget-skills.ts` (five CONFIRMED skill descriptors from FINDINGS.md §2 + registry factory; technical-analysis is REST-backed with interval-aware freshness); `freshness.ts` (profiles: rss 15–60min, community ~15min, technical kline-exact, macro 1–2d lag, market-structure live; all sourced from FINDINGS.md).
- **Behavior preserved:** capability-first routing untouched (registry has no flow concept; tested); market-intel proxy outputs carry proxyBasis into PROXY_EVIDENCE; tested through the full TOOL_RESULT→evidence path; failed results never become evidence; no fabricated fallback data; no trading/execution surface (tested).
- Two M0 files minimally extended (additive only, lock preserved): `tool-result.ts` ToolOutput gained optional `proxyBasis`; `evidence.ts` honors adapter-set proxy labels.
- Verified: `npm run typecheck` clean; **93/93 tests pass** (55 new: MCP 12, REST 4, resilience 10, skill adapters 11, skill integration 11, freshness 7).
- **Not yet built (next):** M2 Flow 1 vertical slice (news-briefing + technical-analysis smallest set per lock §9), then M3 LUI. G1/G2 vendor selection still deferred. Do not proceed to M2 without human go-ahead.
